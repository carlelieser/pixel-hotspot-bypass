# Pixel Kernel Hotspot Bypass - Configuration
# Copy this file to .env and customize for your device
#
# To use: cp .env.sample .env
#
# IMPORTANT: Run ./scripts/detect-device-info.sh to auto-detect the correct values
# for your device, or check your device's current Android version and kernel version.

# ==============================================================================
# DEVICE CONFIGURATION
# ==============================================================================

# Device codename (tegu, tokay, caiman, komodo, comet, etc.)
DEVICE_CODENAME=tegu

# Kernel manifest branch - MUST match your device's Android version
# Format: android-gs-{device}-{kernel_version}-{android_version}[-beta]
#
# Examples:
#   - Android 15 stable: android-gs-tegu-6.1-android15-d4
#   - Android 16 stable: android-gs-tegu-6.1-android16
#   - Android 16 beta:   android-gs-tegu-6.1-android16-beta
#
# To find your version:
#   adb shell getprop ro.build.version.release  # Android version
#   adb shell uname -r                           # Kernel version
MANIFEST_BRANCH=android-gs-tegu-6.1-android16

# Manifest repository URL (usually doesn't need to change)
MANIFEST_URL=https://android.googlesource.com/kernel/manifest

# ==============================================================================
# KERNELSU CONFIGURATION
# ==============================================================================

# KernelSU-Next repository and branch
KERNELSU_REPO=https://github.com/rifsxd/KernelSU-Next
KERNELSU_BRANCH=next

# KernelSU version - hardcoded for Bazel sandbox compatibility
# Leave empty to auto-detect latest from GitHub (requires network access)
# Or specify version manually (e.g., 12882)
KSU_VERSION=12882
KSU_VERSION_TAG=v1.1.1

# ==============================================================================
# BUILD CONFIGURATION
# ==============================================================================

# SoC codename (used in build target)
# Pixel 9 series: zumapro (Tensor G4)
SOC=zumapro

# Bazel configuration name (usually same as device codename)
BAZEL_CONFIG=tegu

# Bazel build target (format: {soc}_{device}_dist)
BUILD_TARGET=zumapro_tegu_dist

# LTO (Link Time Optimization) mode: none, thin, full
# none = fastest build, larger binary
# thin = balanced
# full = slowest build, smallest binary
LTO=none

# Clean build (1 = clean before build, 0 = incremental build)
CLEAN_BUILD=0

# Number of parallel jobs for repo sync (default: number of CPU cores)
# SYNC_JOBS=$(nproc)

# ==============================================================================
# ADVANCED OPTIONS
# ==============================================================================

# Bazel cache behavior
# AUTO_EXPUNGE=0  # Set to 1 to automatically run 'bazel clean --expunge' after defconfig changes

# Custom output directory (relative to project root)
# OUTPUT_DIR=out/${DEVICE_CODENAME}

# Enable verbose build output (1 = verbose, 0 = normal)
# VERBOSE=0
