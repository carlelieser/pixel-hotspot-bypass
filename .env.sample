# Pixel Hotspot Bypass - Configuration Sample
# Copy this file to .env and configure for your device
#
# Quick setup:
#   1. cp .env.sample .env
#   2. Run ./tools/device_info.sh to auto-detect your device settings
#   3. Edit .env with your device-specific values

# ==============================================================================
# Device Configuration
# ==============================================================================

# Device codename (required)
# Run ./tools/device_info.sh to detect automatically
# Common Pixel 9 series codenames:
#   tegu   - Pixel 9a
#   tokay  - Pixel 9
#   caiman - Pixel 9 Pro
#   komodo - Pixel 9 Pro XL
#   comet  - Pixel 9 Pro Fold
DEVICE_CODENAME=tegu

# Kernel manifest branch (required)
# Format: android-gs-{codename}-{kernel-version}-{android-version}[-{variant}]
# Examples:
#   android-gs-tegu-6.1-android15-d4
#   android-gs-tegu-6.1-android16
#   android-gs-tegu-6.1-android16-beta
MANIFEST_BRANCH=android-gs-tegu-6.1-android16

# Google kernel manifest repository URL (required)
MANIFEST_URL=https://android.googlesource.com/kernel/manifest

# ==============================================================================
# KernelSU Configuration
# ==============================================================================

# KernelSU-Next repository URL (required)
# Default uses the official KernelSU-Next fork
KERNELSU_REPO=https://github.com/rifsxd/KernelSU-Next

# KernelSU-Next branch (required)
# Use 'next' for latest stable development branch
KERNELSU_BRANCH=next

# KernelSU version number (required)
# This is hardcoded because Bazel sandbox doesn't include .git for version detection
# Update these when updating KernelSU-Next version
KSU_VERSION=12882

# KernelSU version tag (required)
# Must match the KSU_VERSION above
KSU_VERSION_TAG=v1.1.1

# ==============================================================================
# Build Configuration
# ==============================================================================

# System-on-Chip (SoC) identifier (required)
# Pixel 9 series uses Tensor G4
SOC=zumapro

# Bazel configuration profile (required)
# Should match your DEVICE_CODENAME
BAZEL_CONFIG=tegu

# Build target name (required)
# Format: {SOC}_{DEVICE_CODENAME}_dist
BUILD_TARGET=zumapro_tegu_dist

# Link-Time Optimization mode (required)
# Options:
#   none - Fastest build, larger binary (recommended for development)
#   thin - Balanced optimization and build time
#   full - Maximum optimization, slowest build, smallest binary
LTO=none

# Clean build on each compilation (required)
# Options:
#   0 - Incremental build (faster, recommended)
#   1 - Clean build every time (slower, ensures fresh build)
CLEAN_BUILD=0

# Automatically expunge Bazel cache on config changes (required)
# Options:
#   0 - Manual cache management (you run 'bazel clean --expunge' when needed)
#   1 - Automatically run 'bazel clean --expunge' after config changes
# Warning: Setting to 1 will significantly increase build times after config changes
AUTO_EXPUNGE=0